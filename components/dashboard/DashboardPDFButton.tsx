'use client';

/**
 * Small "Generate PDF" link button used next to Per-Vehicle and YTD table
 * headings on the dashboard. Opens the PDF in a new tab.
 */
export default function DashboardPDFButton({ href, label = 'Generate PDF' }: {
  href:   string;
  label?: string;
}) {
  return (
    <button
      type="button"
      onClick={() => window.open(href, '_blank')}
      className="inline-flex items-center gap-1.5 rounded-lg border border-gray-300 bg-white px-3 py-1.5 text-sm font-medium text-gray-700 hover:bg-gray-50 transition-colors"
    >
      <svg className="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={1.8}>
        <path strokeLinecap="round" strokeLinejoin="round" d="M12 10.5v6m3-3H9m4.06-7.19-2.12-2.12a1.5 1.5 0 0 0-1.061-.44H4.5A2.25 2.25 0 0 0 2.25 6v12a2.25 2.25 0 0 0 2.25 2.25h15A2.25 2.25 0 0 0 21.75 18V9a2.25 2.25 0 0 0-2.25-2.25h-5.379a1.5 1.5 0 0 1-1.06-.44Z" />
      </svg>
      {label}
    </button>
  );
}
